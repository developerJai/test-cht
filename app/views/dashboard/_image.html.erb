 <!-- Full Screen Modal -->
<div id="image-modal-<%=msg.id%>" attr-modal="image-modal-<%=msg.id%>" data-action="click->chats#closeImageModal" class="fixed inset-0 flex items-center justify-center bg-gray-900 bg-opacity-75 hidden">
  <div class="bg-white w-full h-full flex flex-col">
    <div class="p-4 border-b flex justify-between items-center" >
      <button class="text-gray-500 hover:text-gray-700" >Close</button>
    </div>
    <div class="flex-grow p-4 overflow-auto">
      <!-- Modal content -->
      <!-- Check if Cloudinary URL contains `/video/upload/`, which indicates a video -->
      <% if msg.image.include?('/video/upload/') %>
        <video id="video-<%= msg.id %>" controls playsinline muted style="width: 100%;">
          <source src="<%= msg.image %>" type="video/mp4">
          Your browser does not support the video tag.
        </video>
      <% else %>
        <img src="<%= msg.image %>" alt="Image" style="width: 100%;"/>
      <% end %>
    </div>
  </div>
</div>